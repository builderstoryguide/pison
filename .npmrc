# Ensure consistent lockfile behavior
frozen-lockfile=false
prefer-frozen-lockfile=true
auto-install-peers=true
strict-peer-dependencies=false

# For reproducible builds
save-exact=false
save-prefix=^
